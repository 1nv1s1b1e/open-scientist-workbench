{
  "name": "@open-scientist/helix",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "generate-queries": "bun -e \"import('./src/queries.ts').then(() => console.log('queries.json generated')).catch(e => { console.error(e); process.exit(1) })\""
  },
  "dependencies": {
    "@helix-db/helix-db": "^2.0.5",
    "@open-scientist/schema": "workspace:*",
    "@open-scientist/config": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
